/// Mode of the availability of temperature set point.
///
/// [displayOnly] means slider will don work.
///
/// [notDisplay] means setPoint number will not be displayed too.
enum SetPointMode {
  displayAndEdit,
  displayOnly,
  notDisplay,
}